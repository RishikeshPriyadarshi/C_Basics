#include<stdio.h>
int main()
{
    for(int a=0;a<10;a++)
    {
    printf("the value of a is %d\n",a+1);// if we want the value to be printed from 1 to 10 then we use a+1 ,if we use simply a then value will be printed from 1 to 9
    }
    return 0;
}